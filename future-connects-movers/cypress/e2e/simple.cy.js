/// <reference types="cypress" />
describe("Basic Navigation Tests", () => { beforeEach(() => { cy.visit("/"); }); it("should display the sidebar", () => { cy.get("aside").should("be.visible"); cy.get("aside").contains("Future Connects").should("be.visible"); }); it("should toggle sidebar expansion", () => { cy.get("aside").should("have.class", "w-64"); cy.get("button").first().click(); cy.get("aside").should("have.class", "w-20"); cy.get("button").first().click(); cy.get("aside").should("have.class", "w-64"); }); it("should verify URLs after navigation", () => { cy.visit("/", { failOnStatusCode: false }); cy.url().should("include", "/"); cy.visit("/services", { failOnStatusCode: false }); cy.url().should("include", "/services"); cy.visit("/about", { failOnStatusCode: false }); cy.url().should("include", "/about"); }); });
